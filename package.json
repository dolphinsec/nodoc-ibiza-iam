{
  "devDependencies": {
    "openapi-to-postmanv2": "^4.20.1",
    "@redocly/cli": "^1.12.0"
  },
  "scripts": {
    "bundle-spec": "redocly bundle -o build/openapi.yml specification/openapi.yml",
    "build-collection": "npm run bundle-spec && openapi2postmanv2 -s build/openapi.yml -o postman/collections/collection.json -c postman_to_openapi.json"
  }
}
